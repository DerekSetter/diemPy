version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

jupyter_book:
  config: docs/_config.yml

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

